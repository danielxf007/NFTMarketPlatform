import { useEffect, useState } from "react";
const Main_menu = (props) => {
    return (
        <div>
            <button>
            Mint
            </button>
        </div>
    );
};

export default Main_menu;